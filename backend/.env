# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-replace-this-with-a-real-one-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# Commented out by default to fallback to local SQLite. Uncomment and edit to use PostgreSQL.
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hr_flow

# CORS Configuration
# Comma-separated list of origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
